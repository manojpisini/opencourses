---
# ═══════════════════════════════════════════════════════════════════════════════
#  COURSE TEMPLATE — OpenCourses
#
#  Copy this entire directory to engine/courses/<your-slug>/
#  then edit course.md (this file) to build your course.
#
#  Directory layout expected:
#
#    engine/courses/<slug>/
#    ├── course.md                ← you are here (single source of truth)
#    ├── assets/
#    │   ├── images/              ← diagrams, screenshots, banner, thumbnail
#    │   │   ├── banner.png       (1200×630 — course card on the site)
#    │   │   ├── ch-01-diagram.svg
#    │   │   └── ...
#    │   ├── starter/             ← starter code handed to students
#    │   │   ├── ch-01/
#    │   │   │   ├── starter.py
#    │   │   │   └── README.md
#    │   │   └── final-project/
#    │   │       └── template/
#    │   ├── solutions/           ← reference solutions (keep private / separate branch)
#    │   │   └── ch-01/
#    │   │       └── solution.py
#    │   ├── slides/              ← PDF slide decks (per chapter)
#    │   │   └── ch-01-slides.pdf
#    │   └── data/                ← datasets (CSV, JSON, etc.) for data courses
#    │       └── sample.csv
#    ├── course.json              ← auto-generated by course-publish workflow
#    └── contributors.json        ← auto-generated by course-publish workflow
#
#  Referencing assets in this file:
#    thumbnail: "./assets/images/banner.png"
#    url:       "https://raw.githubusercontent.com/manojpisini/opencourses/main/engine/courses/<slug>/assets/slides/ch-01-slides.pdf"
#    starter_code: |              (inline is preferred for code questions)
#      # starter.py contents here
#
# ═══════════════════════════════════════════════════════════════════════════════

meta:
  slug: your-course-slug          # URL-safe, lowercase, hyphens only. e.g. git-mastery
  title: "Your Course Title"
  version: "1.0.0"                # bump on breaking curriculum changes (semver)
  description: >
    One clear paragraph that tells prospective students exactly what they will
    know or be able to do after completing this course. Keep it under 200 words.
  track: foundations              # foundations | languages | web | backend | systems | networks | data | security | architecture | creative | emerging | applied
  difficulty: beginner            # beginner | intermediate | advanced
  tags:
    - tag-one
    - tag-two
  prerequisites: []               # list of course slugs that must be completed first
  estimated_hours: 10
  thumbnail: "./assets/images/banner.png"
  license: "CC BY-SA 4.0"
  repo: "https://github.com/manojpisini/opencourses"

# ─── Contributors ─────────────────────────────────────────────────────────────
# Authors listed here are merged with git-log contributors automatically.
# GitHub username is the canonical identity (same as used for enrollment).
contributors:
  - name: "Your Name"
    github: "your-github-username"
    role: "author"                # author | reviewer | translator | illustrator | maintainer
    email: "you@example.com"      # optional — not shown publicly

# ─── Credits / Attributions ───────────────────────────────────────────────────
credits:
  - title: "Example Book Title"
    url: "https://example.com/book"
    author: "Author Name"
    license: "CC BY-NC-SA 3.0"
    type: book                    # book | paper | video | course | tool | dataset | other

# ─── Chapters ─────────────────────────────────────────────────────────────────
# Each chapter has:
#   - lessons (video / reading / exercise / lab)
#   - chapter_test (required — graded automatically)
#   - project (optional — auto-graded via Docker or manual review)
#
# Students submit chapter tests via GitHub Issues using their @username.
# Enrollment, all chapter tests, the final test, and the certificate all
# tie back to the same GitHub account (@username).
chapters:

  - id: ch-01
    title: "Chapter 1: Getting Started"
    description: >
      A brief description shown in the course outline.

    lessons:
      - id: lesson-1-1
        title: "What is version control?"
        type: video               # video | reading | exercise | lab
        duration_minutes: 12
        url: "https://youtube.com/watch?v=EXAMPLE"
        description: "Introduction to version control concepts and history."

      - id: lesson-1-2
        title: "Installing the tools"
        type: reading
        duration_minutes: 5
        # Reference an asset PDF:
        url: "https://raw.githubusercontent.com/manojpisini/opencourses/main/engine/courses/your-course-slug/assets/slides/ch-01-slides.pdf"

      - id: lesson-1-3
        title: "Hands-on: your first workflow"
        type: exercise
        duration_minutes: 20
        description: "Practice exercise using the provided starter files."
        # Optional in-lesson quick-check quiz
        quiz:
          id: lesson-1-3-quiz
          title: "Quick check"
          pass_score: 60          # % to pass
          max_attempts: 3
          questions:
            - id: q1
              type: mcq
              question: "Which command stages all changes?"
              options:
                - "A. git commit"
                - "B. git add ."
                - "C. git push"
                - "D. git status"
              answer: "B"
              explanation: "`git add .` stages all modified and new files."
              points: 5

    # ── Chapter Test (required) ────────────────────────────────────────────────
    # Submitted via GitHub Issue (quiz-submit template) by the enrolled @username.
    chapter_test:
      id: ch-01-test
      title: "Chapter 1 Test"
      pass_score: 75
      max_attempts: 3
      time_limit_minutes: 30
      questions:

        # MCQ — single correct answer
        - id: q1
          type: mcq
          question: "What does `git init` do?"
          options:
            - "A. Creates a remote repository"
            - "B. Initialises a local Git repository"
            - "C. Clones an existing repository"
            - "D. Resets all changes"
          answer: "B"
          explanation: "`git init` creates the `.git` directory that tracks history."
          points: 10

        # True / False
        - id: q2
          type: true-false
          question: "The staging area is also called the index."
          answer: true
          explanation: "Both terms refer to the same concept."
          points: 5

        # Multi-select — all correct answers must be chosen (partial credit given)
        - id: q3
          type: multi
          question: "Which of the following are Git object types? (Select all that apply)"
          options:
            - "A. blob"
            - "B. tree"
            - "C. branch"
            - "D. commit"
          answer: ["A", "B", "D"]
          explanation: "Git has four object types: blob, tree, commit, tag. Branch is a reference."
          points: 15

        # Short answer — keyword matching (no AI)
        - id: q4
          type: short
          question: "Explain in your own words why Git commits are immutable."
          keywords:
            - SHA
            - hash
            - content
          min_keywords: 2         # at least 2 of the 3 keywords must appear
          sample_answer: >
            Each commit's SHA hash is derived from its content. Changing anything
            would produce a different hash — a new object — leaving the original intact.
          points: 20

        # ── Code question — STATIC test cases ──────────────────────────────
        # Use when all correct implementations produce identical output for
        # a known set of inputs (e.g. "create this file and stage it").
        # Students CAN see these in course.md — hardcoding is still caught
        # because the sandbox runs their actual code, not just checks a string.
        - id: q5
          type: code
          question: |
            Write a single shell command that creates `hello.txt` containing the text
            "Hello, Git!" and stages it for commit.
          language: bash
          starter_code: "# Your command here"
          test_cases:
            - input: ""
              expected_output: "Hello, Git!"
              description: "File content is correct"
              hidden: false
            - input: ""
              expected_output: "A  hello.txt"
              description: "File is staged (git status --short)"
              hidden: false
          points: 20

        # ── Code question — DYNAMIC test generator ───────────────────────
        # Use when the problem is algorithmic and static inputs would let
        # students hardcode answers. The generator script runs with a fresh
        # random seed each submission — students never see the same inputs twice.
        #
        # The generator:
        #   - lives in ./assets/tests/<id>-gen.py  (or .js)
        #   - accepts --seed <int> --count <int>
        #   - prints a JSON array of {input, expected_output, description?, hidden?}
        #   - computes expected_output algorithmically from input
        #
        # Reference solutions in assets/solutions/ are for the instructor only;
        # grading is done by comparing student stdout to generator output — no
        # reference solution is ever executed or compared.
        - id: q6
          type: code
          question: |
            Read a JSON array of integers from stdin.
            Print the sorted array as JSON on a single line.
            Example — input: [3,1,2]  output: [1,2,3]
          language: python
          starter_code: |
            import json, sys
            data = json.load(sys.stdin)
            # TODO: sort and print
          test_generator: "./assets/tests/q5-sort-gen.py"
          test_count: 10        # 10 random cases per submission (last 2 hidden)
          points: 25

    # ── Chapter Project (optional) ─────────────────────────────────────────────
    project:
      id: ch-01-project
      title: "Build a personal dotfiles repo"
      description: >
        Create a public GitHub repo with at least three dotfiles and a README.
        Use conventional commits. Starter template in ./assets/starter/ch-01/.
      deliverables:
        - "Public GitHub repository URL"
        - "At least 5 commits with descriptive messages"
        - "A README.md explaining each dotfile"
      # Auto-graded test cases (optional — remove section for manual review only)
      test_cases:
        - input: "check_repo_public"
          expected_output: "true"
          description: "Repository is public"
          hidden: false
        - input: "count_commits"
          expected_output: ">=5"
          description: "At least 5 commits"
          hidden: true
      manual_review: false
      grading_rubric:
        - criterion: "Repo structure"
          max_points: 20
          description: "Files organised and named correctly"
        - criterion: "Commit quality"
          max_points: 30
          description: "Atomic, descriptive, conventional commits"
        - criterion: "README"
          max_points: 20
          description: "README explains purpose of each dotfile"
      pass_score: 70
      points: 70

  # ─── Add more chapters here ────────────────────────────────────────────────
  # - id: ch-02
  #   title: "Chapter 2: Branching Strategies"
  #   ...

# ─── Certificate ──────────────────────────────────────────────────────────────
# Issued automatically to the @github-username that enrolled, once all
# requirements below are satisfied.
certificate:
  requires_all_chapter_tests: true
  requires_final_project: false   # set true if a final project is mandatory
  min_overall_score: 75           # average across all chapter tests

  # Optional comprehensive final test — only unlocked after ALL chapter tests pass
  final_test:
    id: final-test
    title: "Final Exam"
    pass_score: 80
    max_attempts: 2
    time_limit_minutes: 60
    questions:
      - id: fq1
        type: mcq
        question: "What is the output of `git log --oneline -1`?"
        options:
          - "A. The full commit diff"
          - "B. A single abbreviated commit line"
          - "C. The working tree status"
          - "D. The remote URL"
        answer: "B"
        explanation: "`--oneline` prints one line per commit: short hash + subject."
        points: 10
      # … add more final exam questions

# ─── Automation ───────────────────────────────────────────────────────────────
automation:
  notify_discord: false
  notify_slack: false

# ─── Changelog ────────────────────────────────────────────────────────────────
changelog:
  - version: "1.0.0"
    date: "2026-05-19"
    changes:
      - type: added
        text: "Initial course release"
---

# Your Course Title

> **Track:** Foundations · **Difficulty:** Beginner · **Est. time:** ~10 hours

Write the human-readable course overview here. This markdown body is rendered
as the course landing page on the OpenCourses site. The YAML frontmatter above
drives all automation — GitHub Actions, grading, enrollment, the site data.

## What You'll Learn

- Concept or skill A
- Concept or skill B
- Concept or skill C

## Prerequisites

- No prior experience required (or list specific course slugs)

## How to Enroll

1. Open an [enrollment issue](../../issues/new?template=enroll.yml)
2. Enter `your-course-slug` as the **Course Slug**
3. Your **GitHub account** (`@username`) is your identity — no separate login needed

## Assets

Course assets live in `./assets/`:

| Folder | Contents |
|--------|----------|
| `assets/images/` | Banner image (`banner.png`), chapter diagrams, screenshots |
| `assets/starter/` | Starter code given to students at the start of each chapter |
| `assets/solutions/` | Reference solutions (keep in a private branch or gitignored) |
| `assets/slides/` | PDF slide decks per chapter |
| `assets/data/` | Datasets for exercises (CSV, JSON, etc.) |

## License

Course content is licensed under [CC BY-SA 4.0](../../../../LICENSE-CONTENT).
